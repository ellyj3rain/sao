VERSION = 1,

/*
 * SURVIVOR AWARENESS OVERHAUL - the world decisions, in the game's own screen.
 * Every option here is a live lever. World POLICY lives on this screen;
 * per-person temperament (nerve, appetite, compassion, talkativeness) is
 * emergent from identity by design and deliberately not tunable here.
 */

option SurvivorAwareness.Enable {
    type = boolean, default = true,
    page = SurvivorAwareness, translation = SurvivorAwareness_Enable,
}

/* [C12] Representation is not implementation (DR-017). The internal
 * sentinel (population 0 = derive from the map) stays internal; the
 * screen offers a worded switch and a plain number instead. */
option SurvivorAwareness.PopulationGoverned {
    type = boolean, default = false,
    page = SurvivorAwareness, translation = SurvivorAwareness_PopulationGoverned,
}

option SurvivorAwareness.Population {
    type = integer, min = 1, max = 500, default = 216,
    page = SurvivorAwareness, translation = SurvivorAwareness_Population,
}

option SurvivorAwareness.NewcomersGoverned {
    type = boolean, default = false,
    page = SurvivorAwareness, translation = SurvivorAwareness_NewcomersGoverned,
}

option SurvivorAwareness.Newcomers {
    type = integer, min = 1, max = 1500, default = 500,
    page = SurvivorAwareness, translation = SurvivorAwareness_Newcomers,
}

option SurvivorAwareness.RoadTraffic {
    type = integer, min = 1, max = 12, default = 2,
    page = SurvivorAwareness, translation = SurvivorAwareness_RoadTraffic,
}

/* [C12] Worded steps, not a coded scale - the old RoadPressure double
 * ("0 = none, 5 = an exodus") asked the player to carry a decoder. The
 * step maps to the same internal scalar in one place (the policy
 * reader). */
option SurvivorAwareness.RoadPressureStep {
    type = enum, numValues = 6, default = 1,
    page = SurvivorAwareness, translation = SurvivorAwareness_RoadPressureStep,
    valueTranslation = SurvivorAwareness_RoadPressureStep,
}

option SurvivorAwareness.MaterializeRadius {
    type = integer, min = 20, max = 120, default = 45,
    page = SurvivorAwareness, translation = SurvivorAwareness_MaterializeRadius,
}

option SurvivorAwareness.HibernateRadius {
    type = integer, min = 30, max = 160, default = 70,
    page = SurvivorAwareness, translation = SurvivorAwareness_HibernateRadius,
}

option SurvivorAwareness.RefillDays {
    type = double, min = 0.0, max = 30.0, default = 2.0,
    page = SurvivorAwareness, translation = SurvivorAwareness_RefillDays,
}

option SurvivorAwareness.Desperation {
    type = double, min = 0.5, max = 1.0, default = 0.7,
    page = SurvivorAwareness, translation = SurvivorAwareness_Desperation,
}

option SurvivorAwareness.TrustToCompany {
    type = double, min = 0.2, max = 0.9, default = 0.5,
    page = SurvivorAwareness, translation = SurvivorAwareness_TrustToCompany,
}

option SurvivorAwareness.Voice {
    type = boolean, default = true,
    page = SurvivorAwareness, translation = SurvivorAwareness_Voice,
}

option SurvivorAwareness.DayZero {
    type = boolean, default = false,
    page = SurvivorAwareness, translation = SurvivorAwareness_DayZero,
}

option SurvivorAwareness.Telemetry {
    type = boolean, default = true,
    page = SurvivorAwareness, translation = SurvivorAwareness_Telemetry,
}

option SurvivorAwareness.DormantRisk {
    type = double, min = 0.0, max = 5.0, default = 1.0,
    page = SurvivorAwareness, translation = SurvivorAwareness_DormantRisk,
}

/* [C40] DR-035: default OFF. Holding a prompt means replacing two of
 * another mod's functions with wrappers of ours, which is a reach
 * into their code whatever it is for - so a fresh world does not do
 * it. The words on the screen are unchanged and still true: the
 * player can turn it on, and their own options can do the same job. */
option SurvivorAwareness.HoldNeighbourPrompts {
    type = boolean, default = false,
    page = SurvivorAwareness, translation = SurvivorAwareness_HoldNeighbourPrompts,
}

/* [C17] DR-021's presence layer, first slice: restitution against the
 * crowd ledger. Off by default until the dead census has measured
 * (stated amendable in DR-021's record). */
option SurvivorAwareness.RestoreTakenZombies {
    type = boolean, default = false,
    page = SurvivorAwareness, translation = SurvivorAwareness_RestoreTakenZombies,
}

/* [C40] DR-035: this county stands on its own. Off, nothing of it
 * runs through another survivor mod's code - it is simply never
 * treated as an enemy. On, its people become this county's and its
 * own person menu carries this county's verbs. */
option SurvivorAwareness.NeighbourBridge {
    type = boolean, default = false,
    page = SurvivorAwareness, translation = SurvivorAwareness_NeighbourBridge,
}

/* [C36] DR-031: the game's own broadcasts and papers reach the county
 * on the record's own dates rather than on days since the save began.
 * On the shipped July 9 start nothing changes. */
option SurvivorAwareness.RecordOnCalendar {
    type = boolean, default = true,
    page = SurvivorAwareness, translation = SurvivorAwareness_RecordOnCalendar,
}
