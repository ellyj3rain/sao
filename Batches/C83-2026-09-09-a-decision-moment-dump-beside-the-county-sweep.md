# C83 - A decision-moment dump beside the county sweep

| Field | Record |
| --- | --- |
| Batch | `C83` |
| Date | 2026-09-09 |
| Name | A decision-moment dump beside the county sweep |
| Status | Closed append-only batch - instrument; no mod code |
| Threads | [`T-008`](THREADS.md#t-008), [`T-004`](THREADS.md#t-004) |

## Record

The sister's record 24 ratified where the dataset's rows come from:
a person's own record put to a language model, which decides as that
person, in that situation, among the options actually available. The
operator chose the first target on 2026-09-09 - the work words a
company deals, the founding example of record 21, at a scale of a
hundred-plus rows. A row's first three parts are the county's to
supply and until this batch nothing supplied them: the person exists
in the stores, the situation exists in the belief sets `[C71]` gave
every person, and the options exist at the deal site - but no
instrument took the moment down whole.

## What the instrument is

`tools/county_dump.py` runs the same counties the sweep runs - the
shipped modules, in Kahlua, driven through the mod's own tick
handler, genesis through its own path from the map's own spawn
regions - and where the sweep counts houses, this watches the verb
that deals the work. `SAO.Standing.electLeader` is wrapped from
outside, exactly as the sweep wraps `formCompany`, so the mod carries
no test hook of its own. At every call that finds a roster of two or
more living members, the moment is taken down before the deal runs:
every member's record and traits and conditions and habits and
lessons and age, the census's class and skills, the whole belief set
with each belief's provenance, the trust each member holds toward
each other, the company's creed and claim, and the county hour. Then
the real election runs and what the tree dealt lands beside it -
`designationBefore` and `designationAfter` - as the authored outcome
the dataset exists to replace, kept so a proposed row can be read
against what the tree would have said.

Serialization is generic on purpose: the whole record and the whole
belief set, walked by one table-to-JSON pass with nothing selected,
so the instrument cannot quietly curate what the model sees. A
capture that cannot happen is counted and its first error reported
on its own line, because a harness that fails silently has poisoned
this project's numbers twice.

## What it is not

The choice is not captured, because it is not the county's to make.
That fourth part of a row is the language model's, decided as the
person, and it arrives in the sister repository as a proposal the
operator rules on - AI-written behaviour is not ratified intent, and
a dataset does not become intent by being large. Nothing here is a
border and nothing is in the gate: the tool asserts nothing, passes
nothing and fails nothing, because a border is a point and a county
is a distribution. The dump is data against the install that
produced it and lands outside the tree, like the sweep's world
cache; what makes a row's citation of county and hour answerable is
that re-running the same county name reproduces the same county
(`[C66]` - same code, same map, same spawn points).

## No new border

Tools only - nothing under `mod/` changes and no capability moves.
The designation deal is untouched and reads exactly as it did; the
instrument watches it the way the sweep watches `formCompany`, from
outside, without the tree knowing.