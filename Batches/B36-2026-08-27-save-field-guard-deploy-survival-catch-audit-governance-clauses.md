# B36 - Save-field guard; deploy survival; catch audit; governance clauses

| Field | Record |
|---|---|
| Batch | `B36` |
| Date | 2026-08-27 |
| Name | Save-field guard; deploy survival; catch audit; governance clauses |
| Status | Closed append-only batch - play verification pending (no play receipt) |
| Threads | [`T-030`](THREADS.md#t-030) |
| Superseded entries | `B114`-`B117` |
| Local Git commits | `9bf95b5`, `9e78ef7`, `a04b09f`, `b96e560` - local Git retains the full pre-seam history as engineering evidence |
| Provenance | Joined with immediately adjacent entries: the prior assistant fragmented this single piece of work across the listed raw entries; their substance is consolidated here. That fragmentation is the reason this catalog exists, and this note is its attribution. |

## Record

The discipline layer hardened around saves and silence. The save-field
baseline guard: every field a live save expects is checked against a baseline
commit, with the baseline's own property stated (it only guards what existed
at the baseline) and the two control failures of the new guard diagnosed and
fixed - including an assert whose guessed count would have read as a pass.
Deploy-survival: a live world must survive the deploy, by test. The Java
side's silent catches were all read (forty-three: thirty-seven pure reads with
defaults, six mutations each justified by context), and the record is the
answer - no border was written, because the dangerous and correct shapes are
not statically distinguishable and the count of borders is not a target; the
restraint is the content. And the four governance clauses - enumerate the
idioms first; a control that cannot fail is not a control; a tool that cannot
find its own motivating case measures something else; do not write the
conclusion into the command - were written into GOVERNANCE.md, with citations,
the file itself serving as this unit's record.

This governed record is the portable project history for this unit. Anything it
was found to have asserted against later corrections is corrected in place above
with the correction cited; the raw entries remain reachable through
[`FORMER_LABELS.md`](FORMER_LABELS.md).
