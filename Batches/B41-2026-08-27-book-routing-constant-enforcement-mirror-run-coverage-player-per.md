# B41 - Book routing; constant enforcement; mirror-run coverage; player perception repair

| Field | Record |
|---|---|
| Batch | `B41` |
| Date | 2026-08-27 |
| Name | Book routing; constant enforcement; mirror-run coverage; player perception repair |
| Status | Closed append-only batch - play verification pending (no play receipt) |
| Threads | [`T-008`](THREADS.md#t-008), [`T-030`](THREADS.md#t-030) |
| Superseded entries | `B139`-`B145` |
| Local Git commits | `1d3e113`, `2a053c5`, `3205fa8`, `45cdfb8`, `87f43aa`, `eb92fc2`, `fd59d9c`, `fed29df` - local Git retains the full pre-seam history as engineering evidence |
| Provenance | Joined with immediately adjacent entries: the prior assistant fragmented this single piece of work across the listed raw entries; their substance is consolidated here. That fragmentation is the reason this catalog exists, and this note is its attribution. |

## Record

The gate learned to audit itself. Skill books and knowledge find their work;
constants must be read; comments stopped keeping fields alive. Then the
era's biggest instrument finding: thirty-three mirrors existed, eleven ran,
and twenty-two had been cited in batch records as though writing one were the
same as running it - including the two holding standing laws. All of them
run now, and a border keeps it that way by proving the gate runs the whole
set. Then the finding an era had been blind to: the player's own perception
had never run - one Java instanceof refused the real player for seventy-nine
batches while every instrument read the Lua, which was right. The border that
now guards it reads the three artifacts it does not author (the real player's
extends chain in the jar, the shipped bridge bytecode, and the handoff in the
Lua), with comments blanked before reading and the binding followed by name -
and the six-mutation control matrix, including the trap that compiles clean,
is on the record with the two-shells PATH defect its first control was
suffering from.

This governed record is the portable project history for this unit. Anything it
was found to have asserted against later corrections is corrected in place above
with the correction cited; the raw entries remain reachable through
[`FORMER_LABELS.md`](FORMER_LABELS.md).
