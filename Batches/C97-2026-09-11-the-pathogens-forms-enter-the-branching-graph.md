# C97 - The pathogen's forms enter the branching graph

| Field | Record |
| --- | --- |
| Batch | `C97` |
| Date | 2026-09-11 |
| Name | The pathogen's forms enter the branching graph |
| Status | Closed append-only batch - records and documents; no mod code |
| Threads | [`T-008`](Batches/THREADS.md#t-008), [`T-030`](Batches/THREADS.md#t-030) |

## Record

ZAO's `[A17]` settled six integration rulings:

- the pathogen owns the mutation roll and the roll for form performance;
- crossed is terminal and does not organize around forms;
- retained form traits are state, not new branches;
- capability forms and attribute mutations stack;
- forms are visible facts that enter Perception and change pressure inside
  the living branching graph.

This batch carries that consequence into the shared architecture table.
Speakeasy's record 31 proposes the cross-module row contract that follows
from the same rulings.

## What changed

- `PROJECTS.md` names the integration in the second seam and updates the
  ZAO and Speakeasy status rows.
- `SESSION_STATE.md` advances to this batch.
- `BATCH_LOG.md` gains the `[C97]` row.
- `tools/version_replay.py` classifies `[C97]`.

## No new border

Records and documents only. The doc-currency border covers them. No mod
code, no engine surface.
