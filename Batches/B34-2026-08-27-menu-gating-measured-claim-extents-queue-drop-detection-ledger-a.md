# B34 - Menu gating; measured claim extents; queue-drop detection; ledger and trade audits

| Field | Record |
|---|---|
| Batch | `B34` |
| Date | 2026-08-27 |
| Name | Menu gating; measured claim extents; queue-drop detection; ledger and trade audits |
| Status | Closed append-only batch - play verification pending (no play receipt) |
| Threads | [`T-008`](THREADS.md#t-008) |
| Superseded entries | `B102`-`B107` |
| Local Git commits | `10fe34e`, `21b5b32`, `24a7146`, `3030ce5`, `6ca347b`, `a4eec16` - local Git retains the full pre-seam history as engineering evidence |
| Provenance | Joined with immediately adjacent entries: the prior assistant fragmented this single piece of work across the listed raw entries; their substance is consolidated here. That fragmentation is the reason this catalog exists, and this note is its attribution. |

## Record

The claim work and the queue work, verified end to end. Menu options that
arrive only where they can act; claim extents now measured by the engine's own
building footprint (a house and a survivor's settlement measured by the same
ruler) with outbuildings absorbed whole and the one invented radius fenced off
honestly. The timed-action queue's silent drops (asleep characters, corpse
drags) were found class-wide rather than as instances: pcall had been
reporting success for work never queued, the fallback written for that case
had been unreachable for the project's life, and the fix asks the queue
whether it took the work - with the mirror reading the helper's body rather
than its own model after a control initially could not fail. The two
already-correct audits (the Ledger's rows, the trade legs) were recorded with
the instrument failures that each nearly reported wrong code as right - the
recovery-branch control that could not fire twice, and the premise correction
(the panel rebuilds on a cadence, not per frame) that stopped a wrong
assumption from optimizing correct code into worse code.

This governed record is the portable project history for this unit. Anything it
was found to have asserted against later corrections is corrected in place above
with the correction cited; the raw entries remain reachable through
[`FORMER_LABELS.md`](FORMER_LABELS.md).
