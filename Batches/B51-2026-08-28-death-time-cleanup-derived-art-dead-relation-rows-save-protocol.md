# B51 - Death-time cleanup; derived art; dead-relation rows; save-protocol border

| Field | Record |
|---|---|
| Batch | `B51` |
| Date | 2026-08-28 |
| Name | Death-time cleanup; derived art; dead-relation rows; save-protocol border |
| Status | Closed append-only batch - play verification pending (no play receipt) |
| Threads | [`T-030`](THREADS.md#t-030) |
| Superseded entries | `B186`-`B189` |
| Local Git commits | `67003d7`, `80a2283`, `b8c5580`, `edbf494`, `f439986` - local Git retains the full pre-seam history as engineering evidence |
| Provenance | Joined with immediately adjacent entries: the prior assistant fragmented this single piece of work across the listed raw entries; their substance is consolidated here. That fragmentation is the reason this catalog exists, and this note is its attribution. |

## Record

The county's dead stopped growing quietly. Two functions that nilled exactly
the right tables had never been called by anything but the test harness -
death now calls them through markDead; the per-id caches were censused (eleven
were found; four the author had not known of), the locomotion job's corpse
reference was cleared, and the dormant walk's inner loop stopped including the
graveyard - measured on the engine at 13.29ms falling to 3.19ms per pass at
ten thousand dead, with the break-even and the empty-county cost stated. The
mod's icon and poster became derived artifacts: generated from arithmetic,
regenerated and compared by decoded pixels so a benign recompression cannot
make the border cry. The dead's relation rows were documented as still growing
in the save with the pruning decision placed with the operator because it is
irreversible on a live save. And the one protocol with no checker - the
hibernation pack, Java to save to Java, where a corrupted row is somebody's
inventory - got its pact: keys written against keys handled, both directions,
versions stamped, nested separators matched.

This governed record is the portable project history for this unit. Anything it
was found to have asserted against later corrections is corrected in place above
with the correction cited; the raw entries remain reachable through
[`FORMER_LABELS.md`](FORMER_LABELS.md).
