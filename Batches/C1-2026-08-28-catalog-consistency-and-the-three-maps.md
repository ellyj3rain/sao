# C1 - Catalog consistency and the three maps

| Field | Record |
|---|---|
| Batch | `C1` |
| Date | 2026-08-28 |
| Name | Catalog consistency and the three maps |
| Status | Closed append-only batch - play verification pending (no play receipt) |
| Threads | [`T-030`](THREADS.md#t-030) |

## Record

The published catalog walked against the tree, and the three maps drawn. The
operator's instruction: fix real drift only, no reopened diary, no recut
batches.

**The walk, first half.** PLAYABILITY carried two genesis-era claims later
work replaced (the day-one-mate line predated unit origins; the boot-digest
example predated the digest naming its target's source); the recatalog remap
had bent DR-012's reversal reference onto the absorbing unit and renumbered
its quotation - the operator's words restored verbatim; ROADMAP's census-arc
items shared initials with the epoch namespace and were renamed;
SESSION_STATE's confirmed-fixed flicker item left the waiting list.

**The maps.** MAPS.md: runtime, knowledge, catalog - what a human reads
first. Border 79 (`tools/map_reference_test.py`) holds them: every link
resolves, every named file exists, every cited rule is where the map says.
Its first run caught its own motivating case (border numbers do not live in
check.sh comments); the declarable-not-about-Lua exemption was attempted and
deleted because verifying the mod's file inventory IS reading the mod tree.

**The walk, second half - the index-rot class.** BATCH_LOG's B-era rows all
said 2026-08-26 while the records carry 08-27 and 08-28 (and A8/A9 carry
08-25): twenty-four rows linked files that do not exist, with the date cell
and the link drifted together from the filenames that are ground truth.
Border 79 was extended to the regulatory indexes (BATCH_LOG, VERSION_MAP,
THREADS, FORMER_LABELS: every local link resolves; every BATCH_LOG date is
the linked record's own) and its first extended run found the rest of the
population: 128 more wrong-dated links in FORMER_LABELS. All 152 corrected
mechanically from the filenames. Control: the pre-fix BATCH_LOG at the prior
commit - the real defect, not an adjacent one - flips the verdict.

**The runtime map corrected against the tree.** The bridge edge claimed
three callers where the tree has twelve (including Perception's core
`perceive` call), redrawn as the one Java door the county calls; the interop
law's compression restored to ARCHITECTURE's actual words (verdict strings
*or opaque values* - Lua never iterates or indexes an engine object); the
save story gained its third store (`SurvivorAwareness_Places`, [B39]);
`Events.OnGameStart` -> Neighbours added as the sixth engine event;
Locomotion's label now names its local give-up policies. Volatile figures
(border and mirror counts) left the map for their one owner, SESSION_STATE,
where Border 76 derives them - [B43]'s one-spelling discipline.

**Index defects with single owners.** VERSION_MAP's VU-004/VU-005 overlapped
on A24 against their own former ranges (former A151 resolves to A22, A152 to
A23) - spans corrected to A15-A22 and A23-A29. `tools/mirrors.sh` cited a
pre-recatalog filename for `Batches/OFFLINE-RECEIPTS.md`. MEMORY.md described
`tools/` - the whole border apparatus - as "build and dev scripts".
PLAYABILITY quoted right-click labels that do not exist ("SAO: the county",
"SAO: standing web"); the real surface is the "The County..." submenu,
"County Ledger", and "SAO Debug". NEO.md and BATCH_LOG's preamble stopped
naming the next batch - the tip has one owner and it is BATCH_LOG's last row.

**Verified clean, stated so the walk is repeatable.** THREADS and BATCH_LOG
cross-reference symmetrically in both directions; FORMER_LABELS holds exactly
393 rows (200 former A, 193 former B), no duplicates, every destination a
real record; all 81 record headers identify their own files; SPEECH's
duration table, the one-loop law with `chosen` as the only branch, the boot
digest's exact shape, unit origins, the journal, the twenty outfits, and the
16-option sandbox surface all match the Lua at file and line.

**The gate refused its own batch, and the refusal was an instrument find.**
Committing this close, the pre-commit hook went red where the terminal run
was green: a hook exports `GIT_DIR` and `GIT_INDEX_FILE`, and Border 54's
blinded-tree machinery shells `git ls-files` and runs every mirror from
another directory, inheriting them - in this worktree that flipped the inner
blind verdict from refused to clean. Reproduced deterministically by
exporting the two variables by hand. Fixed at one point for the whole
population: `check.sh` captures the staged file list (the one read that
legitimately needs the hook's index), then scrubs every `GIT_*` variable
before any border runs. Control: the hook-environment run flips from red to
green with the scrub and the terminal run is unchanged; the same instrument
still refuses a genuinely vacuous mirror, per Border 54's own controls.

**Named, not fixed.** Border 76's border figure is the highest number a
mirror prints, not a census (20 and 21 print no verdict; the derivation is
stated in its own docstring). OFFLINE-RECEIPTS cites the pre-recatalog
sequence by its declared convention and its last receipt predates former-B16.
HANDOFF is classified historical and not maintained against the tip.

This governed record is the portable project history for this unit.
