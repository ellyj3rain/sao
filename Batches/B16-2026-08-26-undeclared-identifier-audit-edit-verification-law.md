# B16 - Undeclared-identifier audit; edit-verification law

| Field | Record |
|---|---|
| Batch | `B16` |
| Date | 2026-08-26 |
| Name | Undeclared-identifier audit; edit-verification law |
| Status | Closed append-only batch - play verification pending (no play receipt) |
| Threads | [`T-030`](THREADS.md#t-030) |
| Superseded entries | `B24`-`B26` |
| Local Git commits | `1271fe2`, `3c34d96`, `a977d1e`, `ac634c5` - local Git retains the full pre-seam history as engineering evidence |
| Provenance | Joined with immediately adjacent entries: the prior assistant fragmented this single piece of work across the listed raw entries; their substance is consolidated here. That fragmentation is the reason this catalog exists, and this note is its attribution. |

## Record

The name-guessing class got its instruments and its law. A local used above
its declaration is a nil global read and a block that silently never runs;
the undeclared audit was built (stripping comments and strings), its first run
caught a real crash-in-waiting in the election path, and its false-positive
management (seven engine globals verified then allowlisted) was stated rather
than hidden. The runtime-safety sweep answered whether anything can take a
frame down when it matters - three debug handlers wrapped, cross-layer calls
verified, and a border deliberately declined because a detector without pcall
nesting-awareness would be wrong fourteen times out of seventeen. And the
method law was written: after any multi-part edit, re-read the file and
confirm the text landed before writing a record - a claim in a record is
verified like any other claim. Twenty-four functions claimed across the era
were then all found present, with the two grep artifacts chased down rather
than reported.

This governed record is the portable project history for this unit. Anything it
was found to have asserted against later corrections is corrected in place above
with the correction cited; the raw entries remain reachable through
[`FORMER_LABELS.md`](FORMER_LABELS.md).
