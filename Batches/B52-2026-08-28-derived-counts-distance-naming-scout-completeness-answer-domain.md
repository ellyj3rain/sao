# B52 - Derived counts; distance naming; scout completeness; answer-domain closure

| Field | Record |
|---|---|
| Batch | `B52` |
| Date | 2026-08-28 |
| Name | Derived counts; distance naming; scout completeness; answer-domain closure |
| Status | Closed append-only batch - play verification pending (no play receipt) |
| Threads | [`T-030`](THREADS.md#t-030) |
| Superseded entries | `B190`-`B193` |
| Local Git commits | `336d0cd`, `70a125e`, `7472980`, `c030bf8`, `f661829` - local Git retains the full pre-seam history as engineering evidence |
| Provenance | Joined with immediately adjacent entries: the prior assistant fragmented this single piece of work across the listed raw entries; their substance is consolidated here. That fragmentation is the reason this catalog exists, and this note is its attribution. |

## Record

The terminal B - the era's integrity closed. The canonical document that
exists to say where things stand was found counting wrongly (155 B-batches
claimed at 189; 44 borders at 75), so the figures are now derived from the
tree by a border instead of maintained by hand; the count's asymmetry (one
record that is GOVERNANCE.md) is written into the rule rather than smoothed
over, and the same batch's git-checkout mistake renewed the backup law. Names
were aligned with arithmetic so a forty-tile reach cannot silently become
sixteen hundred (a suffix is a disambiguator here, not a scale - the third
rule attempted and dropped, its cost stated). The last "the gate cannot see
this" line closed: the scout returns ten fields and the Lua reads them all, so
the county can say why it settled where it did, with the pattern proven in the
real VM against the exact strings the engine builds. And the pressure answer's
domain was sealed: the tree had spelled it five ways where the contract is
four - normalized to the documented spelling, with the reader's three drafts
and the control that stayed green on the record as the class's latest
instruction. Two borders caught this batch's own bookkeeping, which is the
gate doing the thing it exists to do on its own author.

This governed record is the portable project history for this unit. Anything it
was found to have asserted against later corrections is corrected in place above
with the correction cited; the raw entries remain reachable through
[`FORMER_LABELS.md`](FORMER_LABELS.md).
