# A23 - Era boundary 0.5; offline equilibrium verification

| Field | Record |
|---|---|
| Batch | `A23` |
| Date | 2026-08-26 |
| Name | Era boundary 0.5; offline equilibrium verification |
| Status | Closed append-only batch - play verification pending (no play receipt) |
| Threads | [`T-008`](THREADS.md#t-008), [`T-009`](THREADS.md#t-009) |
| Superseded entries | `A152`-`A156` |
| Local Git commits | `1eb9782`, `52da823`, `57f98a3`, `930c841`, `f7c033a` - local Git retains the full pre-seam history as engineering evidence |
| Provenance | Joined with immediately adjacent entries: the prior assistant fragmented this single piece of work across the listed raw entries; their substance is consolidated here. That fragmentation is the reason this catalog exists, and this note is its attribution. |

## Record

The county proved its physics without the game. The equilibrium harness ports
the social constants exactly (road warmth with per-pair cooldowns, politick's
weights, company and hostility and feud thresholds, leader peace, the schism
algorithm verbatim) over 60 records across 120 simulated days, seeded and
deterministic: sixteen companies and no mega-company, feuds declared and
peaces made, no universal war and no freeze - with no tuning, the discipline
being tune-only-on-pathology and none showed. Two dynamics were named rather
than hidden: dormant counties ossify by week five (schism needs internal
hostility, which only loaded events mint), and minority creeds collect the
feuds. The day also closed the old era: version 0.5.0.0 by the policy's own
words, the roadmap reconciled with its arcs as built, and the population
fallback aligned to the schema. Then testimony learned the roads (grudges
travel the dormant meetings at their proper weight), the harness ate the new
channel and found its flood (F-040) before any live session could, and the
SAONeeds audit found the locale bug (F-041) that would have starved every
county abroad - one comma against Locale.ROOT.

This governed record is the portable project history for this unit. Anything it
was found to have asserted against later corrections is corrected in place above
with the correction cited; the raw entries remain reachable through
[`FORMER_LABELS.md`](FORMER_LABELS.md).
