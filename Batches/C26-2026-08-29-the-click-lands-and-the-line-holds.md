# C26 - The click lands and the line holds

| Field | Record |
|---|---|
| Batch | `C26` |
| Date | 2026-08-29 |
| Name | The click lands and the line holds |
| Status | Closed append-only batch - awaiting its first live receipt |
| Threads | [`T-004`](THREADS.md#t-004), [`T-006`](THREADS.md#t-006) |

## Record

One play session, three defects of one species: the county REPORTED
what it never VERIFIED, and the operator stood in every gap.

**Talk (R-005).** The operator's report: talking does far less
than even what is advertised. The code agreed on every count: the entire
person sat behind trust 0.3 - fifteen hourly clicks away - so
strangers answered from three stock lines; that reply was then eaten
by the voice module's murmur cooldown and repeat-line guard, built
to space VOLUNTEERED lines and applied to answers; and the talk
cooldown's brush-off went to the log file, so the second click was
[B33] with a timestamp. Fixed on the advertisement's own terms ("a
warning line, a lesson if trust permits"): answers bypass the murmur
guards, the brush-off is spoken ("We just talked."), and smalltalk,
warnings, and identity flow at any trust - what stays earned is the
lessons, the pacts, and what a body admits.

**Dress (R-006, F-054).** Two of the county's own stood naked at the
operator's sink while the log said `dressed=true` - the pcall
believed the CALL; `dressInRandomOutfit()` can return cleanly and
dress nothing. [B34]'s lesson, worn on the skin: outcomes, not
calls. The bridge's `ensureDressed` counts what is actually worn
(`getWornItems().size()`, javap-verified at the declaring class),
retries the wardrobe, falls back to a census outfit, and reports
which happened; the fresh path and the awakened path both consult
it, and the materialize log now carries `worn=` per body. Console
evidence bounded the defect honestly: zero `awakens:` lines and
`0 of his people absorbed at start` - the naked pair were fresh-path
county people, not the neighbour's, not hibernation.

**Ground (R-006, DR-028).** They woke IN the operator's kitchen -
three bodies across two squares of a claimed house, no law at the
wake site. DR-028, both halves as the operator spoke them: lines
delineate at all times, and never exclude life. `wakeSquareFor` now
pushes a wake out through a FOREIGN claim's nearest wall (margin
named: just past the wall, still on their street), moves a home that
predated the claim, spares a person's OWN group ground entirely - a
settled block populates its own garages and houses - leaves the
between-ground free, and lets no two bodies wake on one square.

**Recorded direction (DR-028's rider).** Settlement creation should
learn from REAL bases per the CAO precedent - harvest actual player
saves and multiplayer server raws (desperate and factional building
both) into the spatial logic NPCs build with. The interaction
horizon (build, work, love, wage war, inner strife) is on the
registry as the operator's stated destination; by their measure
the county is nowhere close, and now says so in its own records.

**Border 100** holds all three fixes as one law - nothing reported
that was not verified - with the pre-batch tree as its control,
faulting fifteen ways. Bodies already naked in a running session
stay naked until their next materialization; the fix clothes wakes,
not history.

This governed record is the portable project history for this unit.
