# C109 - Beside this tree, corrected

| Field | Record |
| --- | --- |
| Batch | `C109` |
| Date | 2026-09-12 |
| Name | Beside this tree, corrected |
| Status | Closed append-only batch - records only; no code |
| Threads | [`T-030`](THREADS.md#t-030) |

## Record

The queue's closing note - "Beside this tree" - still said
`../zombie-awareness` G0 was not closed because the Antibodies family
was not installed. That was true when it was written and had been
false for three days: the sister's `[A6]` (2026-09-09) found Antibodies
installed in the Workshop tree (`2392676812`, v1.97, two builds under
`mods/lgd_antibodies/`), read what it exposes - no API, character
`ModData` reachable only by naming the mod, sixty-seven prefixed
sandbox options - and closed G0 on that evidence. Its `[A29]` has since
moved recovery reads to this side's own record entirely, so the named
read `[A6]` left as an open question never shipped either.

A false open gate in the roadmap is the same defect as a false closed
one: it sends the next pass looking for work that does not exist. The
note now states the close and names the batches that did it.

The Speakeasy half of the note was true but stopped at record 29. It
now also states the active cross-module contract, the rows cut against
the event-driven pathogen state (its records 38 and 39), and the
seventy-eight trades-hinge rows that wait on the ruling (its record
30) - the honest shape of what stands beside this tree at this batch.

## What changed

- `ROADMAP.md` - "Beside this tree" corrected: G0 closed at the
  sister's `[A6]`, recovery read from this side's record at its
  `[A29]`; the Speakeasy state carried forward to its records 38 and 39
  with the trades-hinge ruling named as open.
- `BATCH_LOG.md` gains the `[C109]` row; `THREADS.md` T-030 gains
  `C109`; `SESSION_STATE.md` advances to this batch.

## Honest limits

- Nothing was verified here. The close rests on the sister's `[A6]`
  evidence, which this batch quotes and does not re-run, per the
  standing order that verification runs once at the end.
- No other cross-repository claim in this tree was audited for
  staleness; this one was found because it contradicted the sister's
  batch log while composing the end-of-feature-work review. A sweep
  of every cross-reference belongs to the end pass, not to this
  correction.

## Verification

Deferred by the operator's standing order for this pass: no gate,
build, deploy, package, test, or git until the planned feature work
is finished; all of it runs once, at the end.