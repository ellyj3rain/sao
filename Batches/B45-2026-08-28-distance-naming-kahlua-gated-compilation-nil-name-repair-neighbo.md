# B45 - Distance naming; Kahlua-gated compilation; nil-name repair; neighbor narration hold

| Field | Record |
|---|---|
| Batch | `B45` |
| Date | 2026-08-28 |
| Name | Distance naming; Kahlua-gated compilation; nil-name repair; neighbor narration hold |
| Status | Closed append-only batch - play verification pending (no play receipt) |
| Threads | [`T-030`](THREADS.md#t-030) |
| Superseded entries | `B161`-`B164` |
| Local Git commits | `51230c9`, `691ea2e`, `99904aa`, `dc3213a` - local Git retains the full pre-seam history as engineering evidence |
| Provenance | Joined with immediately adjacent entries: the prior assistant fragmented this single piece of work across the listed raw entries; their substance is consolidated here. That fragmentation is the reason this catalog exists, and this note is its attribution. |

## Record

The engine boundary hardened in one run. Distance literals already named were
made consistent (the three-tiles spellings); the gate learned to compile the
mod's Lua with the engine's own Kahlua compiler (a wrong file shape is caught
by the instrument that runs the same rules the game does, and it earned its
keep on real work within batches); the county mourned a person called "nil"
because a global was read that nothing wrote (the globals census now
classifies every global we touch exactly, and every global we write must be
ours - the load-order stakes of writing a neighbour's table are stated); and
the neighbouring framework's narration channel was visibly held - taken at
OnGameStart, counted, logged, restorable, its own settings untouched, because
two systems were describing the same street to the same player and the absence
of a thing must itself be visible.

This governed record is the portable project history for this unit. Anything it
was found to have asserted against later corrections is corrected in place above
with the correction cited; the raw entries remain reachable through
[`FORMER_LABELS.md`](FORMER_LABELS.md).
