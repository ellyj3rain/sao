# Native coordination shadow source

This directory reruns the twenty C81 declared situations against C82's exact
production sources. The authoritative response is still formed by
`SAO.Controller` and committed through `SAO.Organization` before the learned
candidate is submitted. The scene bridge deliberately provides no model
methods, so a shadow refusal is inert and the resulting choices must remain the
same four each of accept, qualify, counter-propose, defer and contest.

The source manifest now binds `SAO_CoordinationInference.lua` alongside the
existing SAO and ZAO owners. Survivor scenes use `SAO.Controller`. Afflicted
and Crossed scenes retain distinct ZAO state while their living human shells,
current activity, capability and source-owned pressure resolve through the
same registered `ZAO.Driver`. No terminal-state, diagnosis, pathogen or diet
field enters the generic decision input.

Border 191 rebuilds all twenty scenes, compares these tracked bytes and repeats
the pressure, execution-registration and executor-attribution mutants. Border
193 separately gives the production inference module a recording bridge and
proves exact snapshot submission, successful Survivor and shared-ZAO-owner
observation, stale revision and owner-state rejection, foreign-bundle and
hidden-field rejection, oversized-table refusal, person-death cancellation and
world-reset cancellation.

Reproduce and compare the exact bytes with:

```text
python tools/coordination_scene_dump.py --out artifacts/audits/c82-native-coordination-shadow-source --check
python tools/coordination_scene_dump_test.py
python tools/coordination_inference_test.py
```

This is controlled headless evidence with synthetic starting conditions. The
learned output is non-authoritative, decline and withdrawal are not represented
by the source family, and no loaded gameplay session is observed.
