# C69 evidence

| Field | Value |
|---|---|
| Timestamp | 2026-09-21 09:09 UTC / 02:09 PST |
| Status | Complete repository gate clean; publication and installation pending |
| Plan | [Producer matrix](PLAN.md) |

C69 lets a non-witness respond to reported household assistance from their own
private evidence. The [batch record](../../../Batches/C69-2026-09-21-recipient-appraisal-after-testimony.md)
states the behavior and remaining boundaries.

| Evidence | Scope | Current result |
|---|---|---|
| Border 180 delivery knowledge | Shipped Perception, Disposition and Knowledge in installed Kahlua | 90 production cases pass; 21 named mutations fail at their controlled defect. |
| Delivery integration | Real request producer and call-site contract | 51 production cases, two static anchors, two known-bad controls and 13 mutations pass. |
| Lua compilation | Every shipped Lua file in normal and debug modes | 77 files, 154 compiler verdicts pass. |
| Sort bounds | Deterministic bounded persisted collections | 23 sort sites and 19 bounded lists pass. |
| Full closure gate | Whole repository | `full-gate.txt` ends with `[check] all borders clean`. |

The first gate rejected a fifth `learnPlace` argument because that governed
acquisition API must retain its established four-argument provenance contract;
C69 now refuses unattributed told claims instead. A second invocation used WSL's
`bash`, which could not resolve the Git Bash Java wrapper. The intended Git Bash
run then required the shipped jar to be rebuilt with the new generated version
stamp. Interrupting that run left a child gate alive; the overlapping output was
discarded, every old check process was stopped, and the final gate ran alone.
Those receipts are preserved in `full-gate-initial.txt`, `full-gate-wsl.txt`,
`full-gate-version.txt` and `full-gate-concurrent.txt`. Only `full-gate.txt` is
the closure result.

The controlled evidence proves data ownership, joining, validation, private
behavioral consumption and refusal paths. Loaded-save behavior and player-visible
acceptance remain separate receipts. No save is loaded for startup verification.
