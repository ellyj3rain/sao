# C64 corrective-integrity and decision-evidence receipts

| Field | Value |
|---|---|
| Timestamp | 2026-09-21 00:03 UTC / 17:03 PST |
| Runtime authority | Installed Project Zomboid Build 42.20.4 jar, Lua and bundled JRE |
| Canonical contracts | `ROADMAP.md` R4/R7/R9/R11/R12; `SUBSTRATE.md` runtime, material and decision-evidence boundaries |
| Speakeasy change | [PR #13](https://github.com/ellyj3rain/zomboid-speakeasy/pull/13), merge `bdfc3e5cfb2a0ec7fe1e482ef0b1b92a4ff82e60` |

## Corrected substrate

C64 treats C49-C63 as suspect implementation and follows each claim through its
producer, durable owner and executable control. Four runtime defects close:

- failed or throwing extension replacement restores the last accepted runtime
  graph and installer registry;
- an Afflicted person's returned visual takes hair/beard timing from that
  returned source rather than the temporary living shell;
- later native loot, rot, movement, disappearance or conflict refreshes or
  retires only an already-owned exact Material source through WorldSources'
  bounded schema-5 queue;
- one selected source is explicitly partial evidence and creates no aggregate
  larder/water claim or settlement storage. Graph and Standing schema 3 remove
  the unsupported C63 outputs without erasing independently grounded evidence.

The retained C63 capability is narrow and useful: one completed exact native
use can attribute one source to held ground, acknowledge once, and reconcile
that source later. It does not establish the rest of the house or complete R9.

## Decision and Speakeasy boundary

Border 181 freezes deterministic decision-time JSON before the real Standing
election and records the immediate result separately. Required-reader, cyclic,
unsupported-value, callback and incomplete-horizon failures withhold the whole
requested dump. Run/county/event identity, unique execution identity,
configuration hash, random state, settings, horizons, schemas and source/model
provenance remain explicit. Publication is one atomic directory rename.

The legacy election exposes no executable options, model choice or later
action-specific consequence. Captures state those omissions and remain
conditioning-ineligible. Speakeasy v3 protects all 190 choices, four historical
derivatives and nine approved world documents by hash; validates exact
run/county/person/event/hour joins and executable-option evidence; refuses
protected destinations; and publishes atomically. The approved intent remains
approved while its old future-conditioned context remains ineligible.

The unmerged former C126 branch contains eight aggregate diagnostics from
different seeds and horizons, without person decisions or complete provenance.
It remains diagnostic history rather than a corpus.

## Review and gate

The review panel's Critical and High findings are closed. The returned-body
Medium is also fixed. The BodySnapshot-to-Population facade remains an explicit
Medium structural deferral because no state loss or wrong result was found and
changing that ownership seam here would widen the corrective batch.

The first full-gate attempts found additional defects before closure: thirteen
stale canonical version headers, an implicit queue-sort bound, two borders that
still invoked retired capture owners, a WorldSources schema-4 expectation and
the prior-version jar. Each was corrected and rerun. The closing gate exits 0
with all 192 scripts clean through Border 181. Focused installed-VM receipts
include 36 native-person mutations, 53 provisioning cases, transactional runtime
reconstruction, WorldSources schema 5 and immutable decision capture.

## Installed startup

The deployment contains 258 files with no missing, extra or different content.
The installed jar matches the gated jar at SHA-256
`EF5CBDE75EEA78825EFA9B86123B3205A37A263273C30F25346822BDFCA8E80A`.
All 44,092 existing save paths, sizes and modification times remain unchanged;
ZAO A37's 29-file content and metadata manifests also remain unchanged.

The final Project Zomboid window is responsive and visibly lists PeekAView,
Staircast, SurvivorAwareness and ZombieAwareness as four active Java mods. The
agent verified `IsoPlayer`, installed the melee, body-scale and loot-density
hooks, loaded `com.sao.Main`, and exposed `SAOJavaBridge` after one transient
pre-Lua attempt. Harness and Population Lua markers loaded with zero SAO failure
lines. Existing third-party fluid-name, translation and attachment errors are
outside SAO. The process is left open.

No save was loaded. This receipt proves deployment and startup, not save-backed
source reconciliation or the feel of the behavior in play.

