# Performed provisioning evidence

Timestamp: 2026-09-21 02:13 UTC / 19:13 PST.

The [plan](PLAN.md) records the producer matrix and authorized scope. C66 extends
the existing source-action owner to native acquisition and storage. The companion
Speakeasy work supplies decision-time knowledge views and unratified authoring.

| Evidence | Result and limit |
|---|---|
| WorldSources transfer ledger | 41 production cases and twelve defect controls; exact deltas, carried signatures, bounded ownership and independent-source concurrency. |
| SourceUse/Needs/Controller/capture lifecycle | 39 production cases and six defect controls in installed Kahlua; native adapters controlled. |
| Native holders | 16 installed-engine checks plus a mutated production proof that fails on simultaneous holders. |
| Source access and material results | Borders 177/180 pass; 53 existing provisioning cases and named mutations retained. |
| Speakeasy authoring | Independent review reran 18 tests and seven join controls; all pass. |
| Protected corpus audit | 190 rows remain ineligible; historical audit counts remain 184 future deaths, 182 future lessons and 163 future beliefs. |

Runtime review repairs: preserve each pending source's evidence, revalidate current
permission and live holder location at mutation, refresh exact private anchors,
retain unavailable recovery, and pass hunger desperation into native acquisition.
Independent review confirms the repairs within its assigned scope.

The full project gate is enforced by the closing commit. Startup and installation
receipts are recorded below after verification. Controlled lifecycle tests and
main-menu startup remain distinct from loaded-save acceptance.


Cross-file review also executes production Perception: an inspected physical
anchor can be recalled for a later exact action while default place readers and
social returns exclude it. Nearby collection routes to the discovered holder;
standing refusal prevents that approach. Concurrent consumption, acquisition and storage preserve
each pending source's observation until its owning action reconciles it. Independent counterexamples now pass.

Speakeasy authoring is published at [PR 14](https://github.com/ellyj3rain/zomboid-speakeasy/pull/14),
merge `5000ec18be546574c9bf21b53f4a2c8d984a4b76`. Its 18 authoring tests and seven
join controls pass; an actual production storage capture also passed the adapter.
DOCX copies are labelled drafts: package integrity is checked, but no page renderer
was available for visual verification.


## Installation and startup

Version `2.8.3.0-pre-alpha` is installed. All 258 deployed files
match source; the jar SHA-256 is `d9134411ca0a96de75c981e351e981bf90e2761aea891477038358d28842dd73`. All 44,092
save-file sizes/timestamps and all 29 ZAO companion files,
including hashes, match the pre-launch baseline. The responsive process was
restored from its inherited minimized state and its main menu inspected. SAO
and ZAO are listed as loaded; no save was loaded. The final launch contains 5064 `ERROR:` lines across stdout/stderr, including repeated imported-animation skeleton diagnostics, external vehicle
templates, two other mods' Lua errors and the already-open ZAO tooltip formatting
issue. Exact duplicate error lines are grouped in the diagnostic receipt. Zero error lines name SAO; this does not establish an
error-free mod stack. See
[startup-verification.json](startup-verification.json), [startup.png](startup.png)
and [startup diagnostics](startup-diagnostics.txt).

The final independent validator passed Border 179, decision capture and all 76
shipped Lua files in normal/debug modes (152 compiler verdicts). Its complete
outputs are [source use](source-use-check.txt),
[decision capture](decision-capture-check.txt) and
[Lua compilation](lua-compilation-check.txt). The corrected
[knowledge-first check](knowledge-first-check.txt) also passes. The mandatory closing commit runs
the full project gate after these focused checks; its successful exit is required
for publication. The client remains at the main menu.

The initial full-gate attempt was cancelled after a Border 6 adjacency finding.
An obsolete five-line comment obscured the queue call from its 15-line lookback;
the comment-only cleanup restores the unchanged check. The final closing commit
reruns the complete gate.


The next complete gate ran from 02:52:39 to 03:09:17 UTC and refused publication.
It reported stale Border 99 finder/private-reader assertions and the overly broad
chunk-admission guard, which broke Border 178's independent-source concurrency.
The Border 99 repair is independently reviewed and controlled per native finder.
The concurrency contract is retained; pending source evidence must be isolated
within a targeted snapshot rather than blocking other containers in the chunk.

The final repair preserves each foreign pending source and its chunk membership
during targeted snapshots. Interleaved acquisition/acquisition, acquisition/storage
and acquisition/consumption now pass. Missing-source and changed-fingerprint
evidence stays with its owner; unscoped and invalid-owner observations retain
their conflict behavior. [Border 178](world-sources-check.txt) preserves the
existing independent-container contract. The final [Border 179 receipt](source-use-check.txt)
passes all 41 ledger cases, twelve mutations, 39 lifecycle cases, six lifecycle
mutations and the installed native holder proof.

Independent final review reproduced the motivating interleaving and a separate
acquire/store case: `original_consume_interleaving=true`,
`distinct_acquire_store=true`, `both_admitted=true`, `both_completed=true`.
The reviewer found no remaining blocker in source isolation.
