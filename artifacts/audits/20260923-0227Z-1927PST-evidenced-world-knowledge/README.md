# C77 acquisition correction evidence

Timestamp: 2026-09-23 02:27 UTC / 19:27 PST.

`example/capture.json` is the corrected authored conversation input.
`example/manifest.json` binds its source files, installed native reading action,
engine jar, calendar probe and native newspaper issue identifiers. `EXAMPLE.md`
explains the scene and what its eight current claims can establish.

C74/C76 evidence remains immutable historical material. The old county-presence
acquisition is superseded by C77 and Speakeasy Record 55. The new completion
executes installed engine Lua over controlled body/item objects; this is an
explicit reading encounter, not observed play or an autonomous reading policy.

Review repaired two reproduced blockers: native info/text keys shared a fixture
typo, and downstream relabelling/resealing could restore the revoked evidence.
The native API probe and complete replay controls now detect those failures.
The Speakeasy output guard also prevents overwriting its correction registry.

The publishing commit runs the complete repository gate. Final gate, merge and
deployment receipts are retained locally beside this tracked evidence. No model
or training row is produced. See the C77 batch for evidence limits and next work.

The initial full gate refused stale release headers, an old installed-version
literal in current session prose, a root-level build log and an unclassified
`ISReadABook` global. Headers now match the replayed patch version, the prior
installation is identified by batch, the log lives with the evidence, and the
native class is classified against the installed action. The failed run is
allowed to finish; publication requires a fresh complete passing hook.
