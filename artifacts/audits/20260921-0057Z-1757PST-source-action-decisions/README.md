# C65 source-action decision receipts

| Field | Value |
|---|---|
| Timestamp | 2026-09-21 00:57 UTC / 17:57 PST |
| Version | `2.8.2.0-pre-alpha` |
| Runtime authority | Installed Project Zomboid 42.20.4 / `b0bbce05d5` and bundled Kahlua/JRE |
| Contract | R11 executable options, separate choice and exact immediate action result; R7 source execution |

Source options contain only the actor's privately observed food/water attempts
at the already selected place. Enumeration is bounded and detached. The runtime
keeps its first-source policy and revalidates the exact chosen descriptor before
reserving it. Capture freezes pre-choice evidence and records the choice and
same-reservation outcome separately. Native measured use remains distinct from
requested quantity. Required-reader failures reject publication; aged-out
results are censored.

`focused-validation.txt` records the independent validator's six passing steps.
`review.json` records both independent reviews and the corrected unreadable-store
defect. `validation.json` identifies the controlled surfaces and mandatory full
gate. The final staged tree passes that gate through the closing commit hook;
publication proceeds only after the commit succeeds.

The initial full gate exposed a stale Border 129 source assertion. It now follows
the item ID and revision through the explicit option into the reservation.
`player-looting-validation.txt` records the corrected pass and a mutation that
destroys the selected identity and is rejected by that same named verdict.
The same run found the county inventory's undeclared loaded-body SourceUse
owner and the person-handoff fixture's old reservation-only interface. Borders
159 and 162 retain their controls after the explicit absence declaration and
fixture update; `gate-repair-validation.txt` holds their focused rerun.

`deployment.json` records exact 258-file installation and unchanged save/ZAO
manifests. `startup.json` records the responsive, visibly loaded main menu and
agent/Lua readiness. Filtered current-launch agent lines are retained alongside
hashes of the local raw startup logs and screenshot. No save was loaded.

The current county sweep reports its absent SourceUse executor explicitly; it
does not generate source-action samples. Runtime choices remain unratified and
conditioning-ineligible. Broader actions, reconstructed private knowledge,
ratified choice authoring and later consequences remain R9/R11/R12 work.

The operator also supplied PZ_Optimization during closure. The separate
[performance reference](performance-reference.md) maps its mechanisms to SAO and
states the evidence needed to evaluate optional compatibility. Its code was
read, with no external implementation copied or installed.
