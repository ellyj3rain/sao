# C76 conversation capture evidence

| Evidence | Scope |
|---|---|
| [Human-readable input](EXAMPLE.md) | Person, circumstances, actual captured catalogue and planned meaning |
| [Capture](example/capture.json) | Exact serialized authored input, namespace, calendar, knowledge coverage, participant records and all host-owner bytes |
| [Manifest](example/manifest.json) | Canonical capture hash, complete production/host source hashes, measured installed calendar and explicit scenario assumptions |
| `full-gate.txt` | Complete repository gate; final exit code recorded at closure |
| Border 189 | 23 production-host tests, multiple defect variants and two shipped-source mutation controls |

The controlled host creates people through Identity/History, records explicit
presence through WorldKnowledge, and passes an authored encounter through
Perception. It loads the same production owner modules used by the county sweep
without advancing a fabricated player conversation or source-use choice. The
installed SAORecord calendar is checked independently. No protected Speakeasy
prose is copied into the mod.

The two read-only review passes reproduced source soft refusal, unsupported
Standing schemas, corrupt/missing/sparse radio claims, owner normalization,
resolved-identity drift, owner replacement and reference reuse. The correction
refuses unready owners before reads, validates strict raw receipt arrays, compares
the whole controlled host state and re-resolves participant/belief identity.
Python assigns references from the canonical complete observation. Review of the
capture/export fixes and its 23-test run found no remaining blocker; the final
radio-array follow-up is covered by the expanded malformed-memory control.

Coverage is successful reading of all current Knowledge topics within this
supported bodyless host. Complete protected-world curation, native loaded-body
capture, task-specific admission, learned inference and live play quality remain
separate work. There is no pending operator review in this batch.

Rebuild the example into a new destination:

```text
python tools/conversation_evidence.py --out NEW_DIRECTORY
python tools/conversation_capture_test.py
```

A new destination preserves immutable prior evidence. Identical observation
bytes yield identical snapshot and claim references. A changed question or
source state yields different references.

The first gate attempt stopped after gate reach rejected the new runner's
implicit unittest exit path. The runner now exposes an explicit nonzero verdict;
all 187 mirrors pass gate-reach validation. The publishing hook runs the full
corrected gate; its log and final deployment receipt are retained locally.

The coverage sort lists also needed an entry in the bounded-sort registry;
all 32 sort sites across 25 lists now have declared bounds. Terminating the
in-progress Windows hook returned an erroneous success status to Git, so the
resulting unpublished local commit was not accepted as verification. The final
amend runs the complete hook without interruption and requires its full verdict
before publication. `interrupted-hook.txt` remains a local diagnostic, not a
passing receipt.

The subsequent uninterrupted run found Border 112 still expecting public
chronicle stamps in personal knowledge. Its fixture and assertions now follow
the received-evidence contract: dated private reception supplies news, an owned
radio and public chronicle alone supply none, and personal lesson dates remain
unchanged. The corrected focused border passes; publication requires a fresh
complete gate after the failed run finishes.

The heard-date correction was independently controlled: replacing `heardDay`
and `heardDate` with event `day` and `date` makes Border 112 refuse.
The first complete run also skipped three paired-project borders because this
worktree lacked a sibling ZAO path. A local directory junction now supplies the
clean existing ZAO checkout at `af3dda0` without changing its source, allowing the
final gate to run those integration checks.
