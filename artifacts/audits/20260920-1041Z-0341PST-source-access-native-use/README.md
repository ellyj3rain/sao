# C62 source access and native-use evidence

| Field | Value |
|---|---|
| Timestamp | 2026-09-20 10:41 UTC / 03:41 PST |
| Runtime authority | Installed Project Zomboid Build 42.20.4 jar, Lua and bundled JRE |
| Canonical contracts | `ROADMAP.md` R6/R7/R9; `SUBSTRATE.md` access, action and producer boundaries |
| Implementation boundary | Private exact source belief through current access, transfer, native use, reconciliation and durable result |

## Resulting behavior

A remembered source may motivate one actor to try. It cannot certify present
access or spend global stock. The durable action retains the exact source,
revision and item while Locomotion reaches the place and the engine-selected
interaction square. Binding then re-resolves the current object and checks
same-floor reach, obstruction, current Standing permission and vehicle-part
permission. Container and vehicle items move through the verified vanilla
transfer; ground items use vanilla grab. The exact carried item enters native
eat or drink-fluid work.

Credit follows a measured physical effect. A completed use publishes one
revision-bound result and stamps that actor's food or water day. Partial native
use publishes its actual quantity as interruption without need credit. Every
post-transfer refusal reconciles the source before releasing ownership. A moved
vehicle carries the reservation to its current chunk; unrelated item changes,
the wrong source chunk or an existing conflict cannot settle as success.

Completed results remain in durable order until the sole `provisioning`
consumer acknowledges them. Delivery uses scalar copies and acknowledgement is
idempotent. C62 does not award settlement material or recognition; R9 must
consume the result against reconciled inventory.

## Actor ownership across pillars

The source reservation owns the person as well as the item transaction. Dormant
life, attrition, settlement, provisioning and encounter approximations skip that
person. The bodyless pathogen encounter/snapshot pass and WorldGenesis graph
pass also skip them. The live controller settles mortality and Crossed transfer
before reconstructing the source phase. A future-schema owner remains opaque and
holds competing producers rather than being read as absent.

## Review and repairs

The first correctness review blocked the batch. It found unrelated source
deltas accepted as completion, private attempts consulting global state,
vehicle access aimed from the vehicle origin instead of the compartment area,
moving vehicles conflicting with their old chunk, post-transfer interruption
escaping reconciliation, wrong-chunk settlement, reload-changing drink
quantity, a structural-only action test and death leaving ownership attached.

Those paths now have executable cases and mutation controls. The structural
review then found competing dormant and daily consumers; the shared actor owner
now gates each. The validation run found tracked-clone, no-game, sort-bound and
prior-border drift; all were corrected before the final gate. `review.json`
records the findings and dispositions.

The first installed startup exposed one further inherited defect: History,
Isolation and Lessons called the alphabetically later shared logger during
module load. The process was rejected and stopped. All eight logger calls in
shared files that load before `SAO_Log.lua` now test the exact logger method.
Border 56 removes History's guard as a rejecting control. The complete gate was
rerun after this repair.

## Mechanical receipts

`gate-final.txt` exits 0 with `[check] all borders clean`: 177 gated test files,
13 other Python entry points, 190 scripts and labels through Border 179. The
installed checks compile 35 Java inputs into 64 classes, compile 75 Lua files in
normal and debug modes, verify version `2.7.16.0-pre-alpha`, match 309 bridge
call sites to 201 methods and prove bridge safety for 207/207 public methods.

Border 179 executes 11 durable-ledger cases and 27 shipped-action cases in the
installed Kahlua VM, with 32 separate static anchors. Borders 162, 168, 173 and
178 exercise cross-owner handoff, WorldGenesis ownership, pathogen ownership and
native hydration/reconciliation. The log-volume border reports 8/8 early logger
calls safe and rejects removal of the History guard.

The deployed install contains 257 files with no missing, extra or differing
content. Its jar SHA-256 is
`7092f686fc7a45f01e577b3d24aad5cc9972fe35b912495d1ab146b08db15c84`.
All 44,092 pre-existing save paths, sizes and modification times remained
unchanged across both deployment/startup attempts.

The final Project Zomboid window is responsive and lists SurvivorAwareness and
ZombieAwareness among four active Java mods. The SAO agent, main class, Harness
and Population markers are present. The final logs contain no Survivor
Awareness stack, compilation error or load refusal. The process is left open.

No save was loaded during this receipt. Save-backed exact source performance and
the feel of the action in play remain observations rather than claims made by
this startup record.
