# C51 person preservation evidence

Timestamp: 2026-09-19 05:44 UTC / 22:44 PST

The motivating pre-repair body failure remains in the immutable
[F-077 evidence](../20260919-0433Z-2133PST-c-substrate-evidence/README.md).
This directory records the repair's verification. The permanent instruments
are tools/person_handoff_test.py, tools/person_snapshot_test.py and
tools/hibernation_pact_test.py, invoked by tools/check.sh.

| Instrument | Actual subject | Boundary |
|---|---|---|
| Border 162 | Production Lua functions in the installed Kahlua VM, including Population, Harness and independent callbacks | Body and bridge doubles inject failures; physical engine teardown frequency is unmeasured. |
| Border 163 | Native inventory, Stats, BodyDamage and XP codecs with real engine characters/items | Headless registry/audio/appearance fixtures; no game world, operator save or play session is used. |
| Border 75 | Production legacy parser and native delegation | Valid v1/v2 compatibility and malformed-input refusal; old summaries cannot recover lost instance data. |

Controls mutate the shipped implementation and demand a failure naming the
changed behavior. Passing these instruments is evidence for their stated
mechanisms. It is not acceptance of the remaining simulation or its dormant
metabolism policy. Source hashes and captured command results accompany closure.

The first full gate failed on three integration details: Border 75's verdict
omitted the prefix recognized by Border 54; pending restoration/removal registries
had no Ledger reader; and their lifetime after death was undeclared. The original
output is retained as `gate-first.log` with its exit-1 receipt. The corrected
implementation reports pending handoffs and retries checked teardown after death.
Ordinary engine corpse handling is unchanged. Border 162 controls the new death
path, including failure retention and refusal to reconstruct a dead person.

| Capture | Meaning |
|---|---|
| `source-hashes.json` | Runtime, verification and installed-engine inputs at closure. |
| `build-final.json` and `.log` | Final native build, exit 0; both distributed jars have the same SHA-256. |
| `native-lifecycle-receipt.json` | Native component probe and twelve compiled source mutations. |
| `merged-adapter-receipt.json` | All production classes loaded from the shipping jar; zero-elapsed restoration and nested inventory ownership. |
| `gate-first.json` and `.log` | Preserved first full-gate failure; not replaced by a later pass. |
| `gate-final.json` and `.log` | Closing full repository verdict, including sixteen Lua handoff mutations. |
| `deployment.json` and `.log` | Deployment result and installed-file comparison when deployment succeeds. |

`verify-shipping-adapter.py` replays the shipping-jar probe using the installed
engine and JDK. Every source adaptation must find its stated seam. By default it
prints its verdict; `--receipt <new-path>` writes a new receipt without replacing
the captured evidence. Permanent Borders 75, 162 and 163 are the controlled
instruments for parser, ownership and native component behavior respectively.

Code review resolved profession defaults overwriting restored experience,
missing native item-processing registration/removal, missing custom perk/trait
acceptance, incoming actions targeting another person's inventory, independent
callbacks advancing a pending body, and pending teardown ownership after death.
Full gameplay, optional character metadata, nutrition, fitness, recipes,
appearance and the positive-elapsed metabolism policy remain unverified here.
